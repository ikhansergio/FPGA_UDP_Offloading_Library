# AXISx8_UDP_TG

AXISx8_UDP_TG - Verilog library that implements UDP datagram traffic generation. IP automatically adds the MAC header, IP4 header, and UDP header, and calculates all necessary checksums.
